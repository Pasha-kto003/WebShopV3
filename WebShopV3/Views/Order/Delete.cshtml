@model WebShopV3.Models.Order
@{
    ViewData["Title"] = "Удалить заказ";
}

<h1>Удалить заказ</h1>

<h3>Вы уверены, что хотите удалить этот заказ?</h3>
<div>
    <h4>Заказ #@Model.Id</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Дата заказа:</dt>
        <dd class="col-sm-10">@Model.OrderDate.ToString("dd.MM.yyyy HH:mm")</dd>
        <dt class="col-sm-2">Пользователь:</dt>
        <dd class="col-sm-10">@Model.User?.Username</dd>
        <dt class="col-sm-2">Тип заказа:</dt>
        <dd class="col-sm-10">@Model.OrderType?.Name</dd>
        <dt class="col-sm-2">Статус:</dt>
        <dd class="col-sm-10">@Model.Status?.Name</dd>
        <dt class="col-sm-2">Сумма:</dt>
        <dd class="col-sm-10">@Model.TotalAmount.ToString("C")</dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Отмена</a>
    </form>
</div>